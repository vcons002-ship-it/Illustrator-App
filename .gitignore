node_modules/
dist/
build/
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
coverage/
.vite/
.turbo/
apps/extension/dist/

# Generated on every build (scripts/build-stamp.mjs) — the running build id the app reads at
# runtime. Regenerated per build, so committing it would churn on every rebuild.
apps/web/public/build.json
